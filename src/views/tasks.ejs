<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Futuristic Task Tracker</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Orbitron', sans-serif;
      background: radial-gradient(circle at top, #1a002b, #000);
      color: #00fff7;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 30px;
      min-height: 100vh;
      overflow-x: hidden;
    }

    h1 {
      font-size: 3em;
      text-transform: uppercase;
      letter-spacing: 3px;
      text-shadow: 0 0 15px #00fff7, 0 0 30px #ff00ff;
      margin-bottom: 25px;
      animation: glow 2s infinite alternate;
    }

    @keyframes glow {
      from { text-shadow: 0 0 10px #00fff7, 0 0 20px #ff00ff; }
      to { text-shadow: 0 0 25px #ff00ff, 0 0 40px #00fff7; }
    }

    .glass-box {
      width: 380px;
      backdrop-filter: blur(15px);
      background: rgba(255, 255, 255, 0.05);
      padding: 25px;
      border-radius: 20px;
      border: 1px solid rgba(0,255,247,0.3);
      box-shadow: 0 0 25px rgba(0,255,247,0.3);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .glass-box:hover {
      transform: scale(1.02);
      box-shadow: 0 0 35px #00fff7, 0 0 45px #ff00ff;
    }

    form input, form textarea, form button {
      width: 100%;
      margin-bottom: 15px;
      padding: 12px;
      border-radius: 12px;
      border: none;
      outline: none;
      font-family: inherit;
      background: rgba(0,0,0,0.6);
      color: #00fff7;
      font-size: 1em;
      box-shadow: inset 0 0 8px #00fff7;
      transition: box-shadow 0.3s ease;
    }

    form input:focus, form textarea:focus {
      box-shadow: 0 0 15px #ff00ff;
    }

    button {
      background: linear-gradient(45deg, #ff00ff, #00fff7);
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    button:hover {
      transform: scale(1.05);
      box-shadow: 0 0 25px #ff00ff, 0 0 35px #00fff7;
    }

    #toggle-future {
      margin-top: 25px;
      padding: 12px 20px;
      border-radius: 15px;
      background: linear-gradient(90deg, #00fff7, #ff00ff);
      border: none;
      font-weight: bold;
      box-shadow: 0 0 15px #00fff7;
      color: #fff;
      text-transform: uppercase;
    }

    #toggle-future:hover {
      transform: scale(1.05);
      box-shadow: 0 0 25px #ff00ff, 0 0 35px #00fff7;
    }

    ul#task-list {
      list-style: none;
      padding: 0;
      margin-top: 30px;
      width: 380px;
    }

    ul#task-list li {
      margin-bottom: 15px;
      padding: 15px;
      border-radius: 15px;
      background: rgba(0,0,0,0.4);
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-left: 4px solid #00fff7;
      box-shadow: 0 0 15px rgba(0,255,247,0.2);
      transition: all 0.3s ease;
    }

    ul#task-list li:hover {
      transform: translateX(5px);
      box-shadow: 0 0 25px #ff00ff, 0 0 30px #00fff7;
    }

    ul#task-list li.completed {
      text-decoration: line-through;
      opacity: 0.6;
      border-left-color: #ff00ff;
      box-shadow: 0 0 15px #ff00ff;
    }

    input[type="checkbox"] {
      transform: scale(1.3);
      cursor: pointer;
      accent-color: #00fff7;
    }

    footer {
      margin-top: auto;
      text-align: center;
      color: #00fff7;
      font-size: 0.9em;
      opacity: 0.7;
    }

    footer span {
      color: #ff00ff;
    }
  </style>
</head>
<body>
  <h1>⚡ Futuristic Task Tracker ⚡</h1>

  <div class="glass-box">
    <form id="add-task-form" action="/tasks" method="POST">
      <input type="text" name="title" placeholder="Task title" required />
      <input type="date" name="date" required />
      <input type="email" name="email" placeholder="Email" required />
      <textarea name="description" placeholder="Task details"></textarea>
      <button type="submit">Add Task</button>
    </form>
  </div>

  <button id="toggle-future">Show Future Tasks</button>

  <ul id="task-list">
    <!-- Example tasks -->
    <li><span>Design Homepage</span><input type="checkbox" /></li>
    <li><span>Database Backup</span><input type="checkbox" checked /></li>
  </ul>

  <footer>
    Built with 💜 by <span>Future Vision</span>
  </footer>
</body>
</html>
