<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Add Movie</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

<style>
body {
    background:#121212;
    color:white;
    font-family:'Poppins', sans-serif;
    margin:0;
    padding:0;
}

/* Navbar */
nav {
    display:flex;
    justify-content:space-between;
    align-items:center;
    padding:20px 40px;
    background:rgba(30,30,30,0.7);
    backdrop-filter:blur(10px);
    position:sticky;
    top:0;
    z-index:100;
}
nav h1 { color:#00ffdd; }
nav a { color:#ff6ec7; text-decoration:none; }

/* Form Box */
.form-box {
    max-width:450px;
    background:#1e1e1e;
    margin:60px auto;
    padding:30px;
    border-radius:15px;
    box-shadow:0 0 25px rgba(0,255,221,0.3);
}

.form-box h2 {
    text-align:center;
    color:#00ffdd;
    margin-bottom:20px;
}

.input-group {
    margin-bottom:18px;
}

.input-group label {
    display:block;
    font-size:14px;
    margin-bottom:5px;
    color:#aaa;
}

.input-group input,
.input-group select {
    width:100%;
    padding:12px;
    background:#2a2a2a;
    border:none;
    border-radius:10px;
    color:white;
    font-size:15px;
}

.input-group input::placeholder {
    color:#777;
}

button {
    width:100%;
    margin-top:20px;
    background:linear-gradient(45deg,#00ffdd,#ff6ec7);
    border:none;
    padding:12px;
    border-radius:10px;
    color:white;
    font-size:18px;
    cursor:pointer;
    box-shadow:0 0 20px rgba(0,255,221,0.5);
}
button:hover {
    transform:scale(1.05);
}
</style>
</head>

<body>

<nav>
    <h1>Add Movie</h1>
    <a href="/movies">← Back to Movies</a>
</nav>

<div class="form-box">
    <h2>🎬 Add New Movie</h2>

    <form action="/collections/add-movies" method="POST">

        <div class="input-group">
            <label>Movie Name</label>
            <input type="text" name="name" placeholder="Enter movie name" required>
        </div>

        <div class="input-group">
            <label>Movie Category</label>
            <input type="text" name="category" placeholder="e.g. Action, Horror, Comedy" required>
        </div>

        <div class="input-group">
            <label>Poster URL</label>
            <input type="text" name="poster" placeholder="Paste direct image URL (.jpg/.png)" required>
        </div>

        <div class="input-group">
            <label>Status</label>
            <select name="status">
                <option value="not watched">Not Watched</option>
                <option value="watched">Watched</option>
            </select>
        </div>

        <button type="submit">Add Movie</button>
    </form>
</div>

</body>
</html>
